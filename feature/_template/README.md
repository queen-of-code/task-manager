# Feature folder template

Copy this folder to `feature/<slug>/` when starting a new Feature, or let `/plan` create `product-spec.md` and `tech-spec.md` from the AI-DLC templates.

Canonical templates also live in `.claude/deps/ai-dlc/skills/spec-management/templates/`.
